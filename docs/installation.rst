Installation
============

You can install ArrayKit via Composer.

.. code-block:: bash

    composer require infocyph/arraykit

Requirements
------------

ArrayKit has the following requirements:

* **PHP 8.4+**

Autoload is PSR-4 based and includes global helper functions from ``src/functions.php``.
